auto {{ var_name }} = Distinct_Builder<{{ in_struct }}, {{ key_struct }}>()
    .withName("{{ op_name }}")   
{% if (par > 1) and (key_lambda) %}
    .withParallelism({{ par }})  
    .withKeyBy({{ key_lambda }})
    .build_keyed();
{% else %}
    .build();
{% endif %}