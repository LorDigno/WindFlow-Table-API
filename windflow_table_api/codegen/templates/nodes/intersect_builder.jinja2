{% if is_all %}
auto {{ var_name }} = Intersect_All_Builder<{{ in_struct }}>()
{% else %}
auto {{ var_name }} = Intersect_Builder<{{ in_struct }}>()
{% endif %}
    .withName("{{ op_name }}")
{% if par > 1 %}
    .withParallelism({{ par }})
    .build_keyed();
{% else %}
    .build();
{% endif %}